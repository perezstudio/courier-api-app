import Testing

@testable import Courier

/// Phase 0 placeholder. Phase 1 fills this out against an in-memory store:
/// CRUD, ordering, cascade deletes, retention pruning, and Keychain
/// round-trips. See REQUIREMENTS.md §9, Phase 1.
@Test
func appTargetIsLinkable() {
    #expect(Bool(true))
}
